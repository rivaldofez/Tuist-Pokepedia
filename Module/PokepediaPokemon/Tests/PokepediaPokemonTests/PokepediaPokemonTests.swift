import XCTest
@testable import PokepediaPokemon

final class PokepediaPokemonTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PokepediaPokemon().text, "Hello, World!")
    }
}
